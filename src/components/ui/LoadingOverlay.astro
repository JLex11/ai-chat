---
interface Props {
  id?: string
}

const { id = 'loading-overlay' } = Astro.props
---

<div
  id={id}
  class="fixed inset-0 bg-black/80 backdrop-blur-sm flex items-center justify-center z-50 hidden"
>
  <div class="bg-slate-800 border border-slate-700 rounded-xl p-6 max-w-md w-full mx-4 shadow-2xl">
    <h2 class="text-xl font-bold text-white mb-2">Descargando modelo</h2>
    <p class="text-slate-400 mb-4" id="loading-model-name">Cargando...</p>

    <div class="relative h-3 bg-slate-700 rounded-full overflow-hidden mb-3">
      <div
        id="progress-bar"
        class="absolute inset-y-0 left-0 bg-gradient-to-r from-blue-600 to-blue-400 transition-all duration-300"
        style="width: 0%"
      ></div>
    </div>

    <div class="flex justify-between text-sm text-slate-400 mb-4">
      <span id="progress-text">0%</span>
      <span id="progress-status">Iniciando...</span>
    </div>

    <p class="text-xs text-slate-500">
      El modelo se guarda en caché. Las próximas cargas serán más rápidas.
    </p>
  </div>
</div>
